задача:

1) получить /создать переменную
    let
    var
2) получить/создать объект(ссылки/текста/цифры/)
3)поместить ранее созданный либо полученный объект в переменную
4) обработать текст
5) отобразить результат

