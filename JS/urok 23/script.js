//let name_="klara";
//console.log(name_);

let number = 8;
let result;
//let -это создать 
// создаем переменную лишь один раз-далее только использование (без let)

number = 3;
//это помещение объекта в переменную
result = number + 1;
console.log(result);


//var-переменные могут быть как обновлены, так переопределены внутри области видимости;
//let-переменные можно обновлять,но не переопределять;
//const- переменные нельзя ни обновлять, ни переопределять;